# Copiar este archivo como .env para cambiar la clave local de MySQL.
MYSQL_ROOT_PASSWORD=cinego_dev_password
DB_PASSWORD=cinego_dev_password

# Secret usado para firmar los JWT (HS256 requiere al menos 32 caracteres/256 bits).
JWT_SECRET=change-me-in-env-please-use-a-secret-of-at-least-32-chars
