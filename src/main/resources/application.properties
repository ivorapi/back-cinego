
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3307/cinego?serverTimezone=UTC}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:cinego_dev_password}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
jwt.secret=${JWT_SECRET:change-me-in-env-please-use-a-secret-of-at-least-32-chars}
jwt.expiration=86400000
